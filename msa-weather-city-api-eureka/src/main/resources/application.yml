spring:
  application:
    name: msa-weather-city-api-eureka

server:
  port: 8080
    
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
  instance:
    appname: ${spring.application.name}
    hostname: localhost