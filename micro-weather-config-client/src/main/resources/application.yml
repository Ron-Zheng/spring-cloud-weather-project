spring:
  application:
    name: micro-weather-config-client
  cloud:
    config:
      profile: dev
      uri:
      - http://localhost:8888/

server:
  port: 8081

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
      